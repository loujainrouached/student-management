pipeline {
    agent any

    tools {
        maven 'MAVEN'
    }

    environment {
        DOCKER_IMAGE = 'loujainrouached/student-app'
        DOCKER_TAG   = "${BUILD_NUMBER}"
        NAMESPACE    = 'devops'
        GIT_REPO     = 'https://github.com/loujainrouached/student-management.git'
    }

    stages {

        stage('Checkout') {
            steps {
                echo '📥 Cloning repository...'
                git branch: 'main', url: "${GIT_REPO}"
            }
        }

        stage('Build with Maven') {
            steps {
                echo '🔨 Maven build...'
                dir('student-management') {
                    sh 'mvn clean package -DskipTests'
                }
            }
        }

        stage('SonarQube Analysis') {
            steps {
                echo '🔍 SonarQube analysis...'
                dir('student-management') {
                    withSonarQubeEnv('Sonarqube') {
                        sh 'mvn sonar:sonar || true'
                    }
                }
            }
        }

        stage('Build Docker Image (Minikube)') {
            steps {
                echo '🐳 Building Docker image directly inside Minikube...'
                dir('student-management') {
                    sh """
                        minikube image build -t ${DOCKER_IMAGE}:${DOCKER_TAG} .
                    """
                }
            }
        }

        stage('Deploy to Kubernetes') {
            steps {
                echo '☸️ Deploying to Kubernetes...'
                sh """
                    kubectl set image deployment/studentmang-app \
                    studentmang-app=${DOCKER_IMAGE}:${DOCKER_TAG} \
                    -n ${NAMESPACE}

                    kubectl rollout status deployment/studentmang-app \
                    -n ${NAMESPACE} --timeout=10m
                """
            }
        }

        stage('Verify Deployment') {
            steps {
                echo '✅ Verifying deployment...'
                sh """
                    kubectl get pods -n ${NAMESPACE}
                    kubectl get svc -n ${NAMESPACE}
                """
            }
        }
    }

    post {
        success {
            echo '✅ PIPELINE SUCCESS 🎉'
        }
        failure {
            echo '❌ PIPELINE FAILED'
        }
    }
}
