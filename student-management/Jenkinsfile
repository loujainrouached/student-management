pipeline {
    agent any

    tools {
        maven 'MAVEN'
    }

    environment {
        NAMESPACE = 'devops'
        KUBECTL = '/usr/local/bin/kubectl' // <-- ajouter chemin kubectl
    }

    stages {

        stage('Checkout') {
            steps {
                echo '📥 Checkout source code'
                checkout scm
            }
        }

        stage('Build with Maven') {
            steps {
                echo '🔨 Maven build'
                dir('student-management') {
                    sh 'mvn clean package -DskipTests'
                }
            }
        }

        stage('SonarQube Analysis') {
            steps {
                echo '🔍 SonarQube analysis'
                dir('student-management') {
                    withSonarQubeEnv('Sonarqube') {
                        sh 'mvn sonar:sonar || true'
                    }
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                echo '🐳 Build Docker image'
                dir('student-management') {
                    sh 'docker build -t student-management-app:1.0 .'
                }
            }
        }

        stage('Kubernetes Deploy') {
            steps {
                echo '☸️ Deploy to Kubernetes'
                sh '''
                    ls -l
                    ls -l student-management
                    ls -l student-management/k8s

                    ${KUBECTL} apply -f student-management/k8s
                    ${KUBECTL} get pods -n ${NAMESPACE}
                    ${KUBECTL} get svc -n ${NAMESPACE}
                '''
            }
        }
stage('Wait for Spring Boot') {
    steps {
        echo "Waiting for Spring Boot app to be ready..."
        // Ici on fait juste un sleep, simple et rapide
        sh 'sleep 20'
    }
}

stage('Create Department') {
    steps {
        echo "Creating a Department via REST API..."
        sh '''
        # Adresse fixe et port de ton service NodePort
        SERVICE_IP=192.168.49.2
        NODE_PORT=32639

        curl -X POST "http://${SERVICE_IP}:${NODE_PORT}/department/createDepartment" \
             -H "Content-Type: application/json" \
             -d '{"name": "Finance", "location": "Sfax"}'
        '''
    }
}
    }


    post {
        success {
            echo '✅ PIPELINE TERMINÉ AVEC SUCCÈS 🎉'
        }
        failure {
            echo '❌ PIPELINE ÉCHOUÉ'
        }
    }

} // <-- fin pipeline
